---
trigger: always_on
---

Before submitting your changes, always run `just build` which runs all code validation checks and ensures that the project builds successfully.

Make sure to address any errors before submitting your changes.
